#include<bits/stdc++.h>
using namespace std;

int fun(int l, int r, string s){

    if(l==n)return 0;
    1 + fun(l+1, l+2, s);
    for(int i = 0;i<n;i++){

        if(sub)
    }
}

int main(){

    string s;
    cin>>s;
    
    fun(0, 1, s);
}